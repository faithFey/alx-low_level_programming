#include<stdlib.h>
#include<stdio.h>

/**
 *main -program that prints all single digit numbers of base 10  from 0.
 *Return: return(0) successful.
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	return (0);
}
